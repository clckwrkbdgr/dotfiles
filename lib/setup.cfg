[unittest]
data_dir = ~/.cache/pytest/config

# TODO: clckwrkbdgr/winnt/shell.py - can be imported only on Windows.
[coverage:run]
omit = /usr/*
	clckwrkbdgr/winnt/shell.py
data_file = ~/.cache/pytest/config/coverage

[coverage:report]
skip_covered = True
fail_under = 100

[coverage:html]
directory = .htmlcov
