[unittest]
data_dir = ~/.cache/pytest/config

[coverage:run]
omit = /usr/*
data_file = ~/.cache/pytest/config/coverage

[coverage:report]
skip_covered = True
fail_under = 100

[coverage:html]
directory = .htmlcov
